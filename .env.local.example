# DoorFlow OAuth Application Credentials
# Get these from developer.doorflow.com → Add New Application
# 1. Create a new OAuth application
# 2. Set redirect URI to: http://localhost:3000/api/auth/callback
# 3. Copy the Client ID and Client Secret below

DOORFLOW_CLIENT_ID=your_client_id_here
DOORFLOW_CLIENT_SECRET=your_client_secret_here

# DoorFlow API URL (optional - defaults to https://api.doorflow.com)
# DOORFLOW_API_URL=https://api.doorflow.com

# Application URL (optional - defaults to http://localhost:3000)
# Only set this if deploying to production or using a different port
# NEXT_PUBLIC_APP_URL=http://localhost:3000
